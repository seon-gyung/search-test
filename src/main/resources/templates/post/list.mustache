<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>

<div class="container mt-3">
  <h1>게시물 11개 뿌리기</h1>
  <form class="d-flex mt-5 mb-5" action="/" method="get">
    <input class="form-control me-2" type="text" name="keyword" placeholder="Search">
    <button class="btn btn-primary" type="submit">Search</button>
  </form>
  {{#lists}}
  <div class="card mb-3" style="width:auto">
    <div class="card-body">
      <h3 class="card-title" mt-3>제목 : {{title}}</h3>
      <p class="card-text" >번호 : {{id}}</p>
      <p class="card-text">내용 : {{content}}</p>
      <p class="card-text">작성일 : {{createDate}}</p>
      <a href="#" class="btn btn-primary">상세보기</a>
    </div>
  </div>
  {{/lists}}
  <br>

</body>
</html>
